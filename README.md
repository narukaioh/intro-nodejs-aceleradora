1) crie um endpoint GET que retorne seu nome para o browser
2) crie um endpoint GET que envie nome e sobrenome, e retorne a seguinte mensagem:
    "Seu nome completo é: PrimeiroNome SegundoNome"
3) crie um endpoint para cada operação matemática, onde tenha que
fazer a soma, subtração, divisão e multiplicação de dois numeros inteiros.
4) crie um endpoint que calcule a media de 3 notas enviadas pela URL
